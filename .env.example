# Server Configuration
PORT=7070

# Session Secret (change this to a random string in production)
SESSION_SECRET=bookit

# Goodreads API (optional - defaults provided)
GOODREADS_KEY=your_goodreads_api_key
GOODREADS_SECRET=your_goodreads_api_secret

# MongoDB Connection
# Option 1: Use local MongoDB (default)
# Make sure MongoDB is installed and running: brew services start mongodb-community
# MONGODB_URI=mongodb://localhost:27017/bookit

# Option 2: Use MongoDB Atlas (cloud)
# Create a free cluster at https://www.mongodb.com/cloud/atlas
# Then replace with your connection string:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/bookit?retryWrites=true&w=majority
